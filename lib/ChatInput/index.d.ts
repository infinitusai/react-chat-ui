import React from 'react';
interface ChatInputProps {
    inputStyles?: object;
    inputPlaceholder?: string;
}
declare const ChatInput: (props: ChatInputProps) => React.JSX.Element;
export default ChatInput;
